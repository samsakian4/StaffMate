// Supabase project configuration.
// The anon/public key is safe to ship client-side — real protection comes
// from Row Level Security (RLS) policies on the database (owner_id = auth.uid()).
export const SUPABASE_URL = 'https://icaqywcbdvszvaprpxoy.supabase.co';
export const SUPABASE_ANON_KEY = 'REPLACE_WITH_ANON_KEY';
